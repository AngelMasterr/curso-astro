---
interface Props {
	id: string
	name: string
	img: string
	success: boolean
	flightNumber: number
	details: string
}
const { id, details, img, success, flightNumber } = Astro.props
//const successText = success ? "Exito" : "Fracaso"
---
